## Summary

<!-- What does this change do, in one or two sentences? -->

## Motivation

<!-- Why? Link an issue if there is one (`Closes #123`). -->

## Checklist

- [ ] `uv run pytest` passes
- [ ] `uv run ruff check .` and `uv run ruff format --check .` pass
- [ ] Tests added/updated for any new behavior (and they're **offline** — mocked via respx, no live network)
- [ ] `CHANGELOG.md` updated under `[Unreleased]` for user-visible changes
- [ ] `README.md` / `docs` updated if behavior changed
- [ ] No new dependencies added without justification

## Notes for reviewer

<!-- Anything they should pay attention to. -->
